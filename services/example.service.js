// Business logic for the application
const db = require('../config/db');

module.exports = {
    // Example service function
    getSampleData: async () => {
        return { data: 'Sample service data' };
    }
};
